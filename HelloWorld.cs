using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HelloWorld : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // tenemos que ponerlo abajo de la linea start entre el corchete poner print parentesis y las 2  comillas poner hello ''tu nombre''y cerrar la comilla el parentesis y no te olvides de poner el punto y coma que te veo -_-
        print("hello patatita");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
